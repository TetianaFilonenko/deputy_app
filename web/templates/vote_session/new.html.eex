<h2>New vote session</h2>

<%= render "form.html", changeset: @changeset,
                        action: vote_session_path(@conn, :create) %>

<%= link "Back", to: vote_session_path(@conn, :index) %>
